<!--根据某些条件删除-->
  <delete id="deleteByEntity" parameterType="com.nico.TestEntity"> 
      delete from TestEntity where  1 = 1
        
	<if test="id != null" >
	          and `id` = #{id,jdbcType=VARCHAR}
	      </if>
	
	<if test="age != null" >
	          and `age` = #{age,jdbcType=INT}
      </if>


  </delete>